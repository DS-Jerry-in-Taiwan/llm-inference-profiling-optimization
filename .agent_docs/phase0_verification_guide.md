用途：提供 Agent 自我檢查與 User 驗收的標準。

text
# Phase 0 驗證指南

## ✅ Agent 自我檢查清單

### 結構完整性
- [ ] 所有資料夾（src, results, notebooks, docs, models）已建立
- [ ] `src/` 包含 `__init__.py`
- [ ] 所有 Python 模組檔案已建立（即使是空檔案）
- [ ] `results/` 包含 `traces/` 和 `charts/` 子目錄

### 檔案正確性
- [ ] `requirements.txt` 包含至少 10 個套件
- [ ] `.gitignore` 包含 Python、模型、結果相關規則
- [ ] `README.md` 包含 Quick Start 章節
- [ ] `setup_check.py` 包含 5 個獨立檢查函數

### 功能驗證
- [ ] `python setup_check.py` 無語法錯誤且能執行
- [ ] `pip install --dry-run -r requirements.txt` 無錯誤
- [ ] Git 初始化成功（如適用）

### 品質標準
- [ ] 所有檔案使用 UTF-8 編碼
- [ ] Python 檔案包含基本 docstring
- [ ] 文件使用正確的 Markdown 語法

---

## 🧪 驗收測試案例

### Case 1: 結構驗證
**指令**: `tree -L 2 llm-inference-optimization/`
**預期**: 顯示完整的目錄樹，包含所有預定義的檔案與資料夾。

### Case 2: 依賴驗證
**指令**: `pip install --dry-run -r requirements.txt`
**預期**: 解析所有套件版本無衝突。

### Case 3: 環境驗證
**指令**: `python setup_check.py`
**預期**: 輸出格式化的檢查報告，顯示 Python、PyTorch、Transformers 等狀態。

### Case 4: Git 忽略驗證
**指令**: 建立 `__pycache__/test` 後執行 `git status`
**預期**: `__pycache__` 不應出現在 untracked files 中。

---
