用途：記錄 Phase 0 完成狀態，移交給 Phase 1 Agent 的資訊。

text
# Phase 0 → Phase 1 移交文件

## 📊 專案狀態概覽

**階段**: Phase 0 (Project Setup)  
**狀態**: ✅ 已完成  
**日期**: 2025-12-15

---

## 🛠️ 環境資訊

- **Python 版本**: 3.11.9
- **PyTorch**: 2.9.1+cu128
- **CUDA**: Not Available
- **已安裝套件**: 見 `requirements.txt`

---

## 📂 交付內容

1. **專案結構**: 完整建立，包含 `src/`, `results/`, `notebooks/`, `docs/`, `models/`。
2. **設定檔**: `requirements.txt`, `.gitignore` 已配置。
3. **文件**: `README.md` 初版已建立。
4. **工具**: `setup_check.py` 可用於持續驗證環境。
5. **代碼**: `src/` 下的 `.py` 檔案為空（待 Phase 1 實作）。

---

## ⚠️ Phase 1 (Baseline) 注意事項

給 **Development Agent** 的建議：

1. **模型選擇**:
   - 預設建議使用 `gpt2` (124M) 或 `distilgpt2` 進行開發測試，速度較快。
   - `llama-3.2-1b` 可作為進階測試選項。

2. **開發順序**:
   - 優先實作 `src/baseline.py` 的 `load_model` 和 `inference` 函數。
   - 確保 `inference` 函數支援 `use_cache` 參數。

3. **數據記錄**:
   - 建議將 baseline 數據儲存為 JSON 或 CSV 格式，方便後續比較。
   - 使用 `time.perf_counter()` 進行高精度計時。

4. **硬體適配**:
   - 代碼需檢查 `torch.cuda.is_available()`，自動選擇 device。
   - 在 CPU 環境下，生成 token 數建議設為 10-20 以節省時間。

---

## 🔜 下一步行動

**目標**: Phase 1 - 環境建置 & Baseline  
**主要任務**:
1. 安裝套件
2. 跑通最小推論範例
3. 測量 Baseline Latency (with/without KV-cache)
